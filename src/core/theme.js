import { DefaultTheme } from 'react-native-paper'

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#000000',
    primary: '#FF3320',
    secondary: '#414757',
    error: '#f13a59',
  },
}

export default theme;